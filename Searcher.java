// Find levenshtein dist. - at least the diff of sizes of the two strings
// At most the length of the longer strings
// It is zero if both strings are equal
// No greater than the sum of differences from a third string

// If the length is zero, throw an error
// If the last characters match,

// Utilize a 'cost' system

public class Searcher {
  int cost;

  public search(origin, costLim) {
    
  }
}
